---
tags: [task, feature, casino, crash]
id: PB-235
epic: J — Crash / Casino
priority: P2
status: todo
depends_on: [PB-231]
---

# PB-235 — Casino chat: ring buffer, rate limits, moderation, UI box

Plan: `plans/crash/` (decisions C1–C17).

## Scope (plans/crash/03 C13)
- `post_casino_chat(text)` (signed-in, 200 chars, charset-validated,
  1 msg/5s/principal), `get_casino_chat(since_id)`; 500-msg ring (mem 65).
- `admin_mute_chat(principal, until)` + `admin_delete_chat(id)`,
  audit-logged; muted users see own messages locally only.
- UI: chat rail on hub/crash (and poker table page), plain-text rendering,
  model badge flavor for poker-agent owners.

## Acceptance
- Rate-limit + length + charset tests; mute semantics; injection fixture
  corpus renders inert; ring eviction; vitest for the box.
