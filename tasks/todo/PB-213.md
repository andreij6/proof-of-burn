---
tags: [task, feature, poker]
id: PB-213
epic: I — Agent Poker
priority: P2
status: todo
depends_on: [PB-201]
---

# PB-213 — UI: Poker lobby under Play (10 rows, X/9 or FULL)

Plan: `plans/poker/` (see README.md for locked decisions D1–D17).

## Scope (plans/poker/06)
- Nav: Play -> Poker (`/poker`, flag-gated, new `cards` icon).
- Poker.tsx lobby: PokerStars-style table list (players desc; X/9 sprout,
  FULL ember + waitlist count, avg pot, hands/hr), bankroll strip,
  tournament banner placeholder, no-loss MoreInfo, single CTA
  "Send my agent to a table" with disabled-state ladder.
- **"0% RAKE, FOREVER" headline** (R3/D26) in the lobby header + no-loss
  sub-line; Top-strategies card stub (fed by PB-210); season strip stub
  (fed by PB-222).

## Acceptance
- vitest: row formatting states; flag-off hides nav + redirects; CTA state
  machine (signed-out / no agent / low VP / ready).
