---
tags: [task, feature, casino, crash]
id: PB-238
epic: J — Crash / Casino
priority: P2
status: todo
depends_on: [PB-234, PB-235]
---

# PB-238 — UI: bustabit-style crash screen + Casino hub + no-loss copy

Plan: `plans/crash/` (decisions C1–C17).

## Scope (plans/crash/03)
- Casino hub page (banner doctrine C16, two game cards, chat).
- Crash screen: canvas curve from run_started_at (parity with canister
  m(t) table via shared fixture), countdown ring, crash flash/shake, MOON
  treatment at 100x; history bar w/ verify dialog (client recompute);
  bet panel (rails, VP line, latency-honesty line, CASH OUT live button);
  players list w/ live cashout toasts; my-result strip + stop-loss
  distance meter; auto-pilot dock; SFX on arcade WebAudio (global mute).

## Acceptance
- vitest: curve parity fixture, verify recompute, bet-panel state machine,
  history formatting, C16 banner snapshot on every casino route; manual
  local play-through (human bet + manual cashout + autopilot session).
