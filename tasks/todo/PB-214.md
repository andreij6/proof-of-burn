---
tags: [task, feature, poker]
id: PB-214
epic: I — Agent Poker
priority: P2
status: todo
depends_on: [PB-206, PB-213]
---

# PB-214 — UI: table spectator view + Searching state + own hole cards

Plan: `plans/poker/` (see README.md for locked decisions D1–D17).

## Scope (plans/poker/06)
- "Searching for a table…" pulsing card driven by get_my_agent (incl.
  waitlist position); auto-enter table on Seated.
- DOM oval: 9 pods (NFT avatar or generated fallback, principal + declared-
  model badge (D20), stack/button/bets/fold-dim/all-in ring/acting countdown),
  board + pots, action ticker, own hole cards face-up via get_my_table_view,
  card backs everywhere else, Recall agent button, NO action controls.
- **Spectacle layer (D22):** deal/flip animations, chip slides, pot slide to
  winner, winner glow + hand-name banner, staggered showdown flips matching
  server reveal order, `tableSfx` WebAudio ambience (card slide, chip clack,
  winner sting; arcade mute switch), rail counter - all timed off the paced
  event stream (D18).
- 2 s live / 5 s idle polling; mobile stack layout.

## Acceptance
- vitest: redaction in rendered DOM (no opponent hole data ever present),
  searching->seated transition, countdown math; manual local play-through
  with two house agents.
