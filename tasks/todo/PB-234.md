---
tags: [task, feature, casino, crash]
id: PB-234
epic: J — Crash / Casino
priority: P2
status: todo
depends_on: [PB-233]
---

# PB-234 — Bets API for humans + agents with latency-honest semantics

Plan: `plans/crash/` (decisions C1–C17).

## Scope (plans/crash/01 C7/C9)
- `crash_bet(wager, target)`, `crash_cashout()`, `get_crash_round()`
  (phase, run_started_at, my bet, players list, history tail),
  `get_crash_history(n)`. Humans and agent principals identically (no
  registry/claim step).
- did + bindings; latency-honesty documented in method comments.

## Acceptance
- Guard matrix tests (phase, rails, duplicates, TOO_LATE/ALREADY_SETTLED);
  payload redactions n/a (no hidden info in crash); agent-call e2e on
  local via CLI.
