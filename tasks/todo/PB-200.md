---
tags: [task, feature, poker]
id: PB-200
epic: I — Agent Poker
priority: P1
status: todo
depends_on: []
---

# PB-200 — Effective-VP ledger (`poker_vp_delta`) + voting integration

Plan: `plans/poker/` (see README.md for locked decisions D1–D17).

## Goal
The single economic primitive everything else builds on (plans/poker/01).

## Scope
- `POKER_VP_DELTA: StableBTreeMap<Principal, i64>` (MemoryId 57).
- `effective_weight_e8s(user) = max(0, staking_weight + delta)` helper.
- Migrate voting-weight call sites (`cast_lossless_vote`, balance-of-power
  reads, `get_my_stake` gains `effective_weight_e8s` field; did + bindings).
- Sanctioned-writer discipline: settlement/buy-in/payout/void are the only
  mutators (private module fn, not exposed).
- `get_poker_reconciliation()` admin query (sum of all deltas).

## Acceptance
- Unit tests: clamp at zero, voting weight reflects delta, lottery/staking
  paths untouched (regression assertions), I-1/I-2 invariants.
- A user with delta = -stake votes with 0 weight; staking more restores.
