---
tags: [task, epic-g, docs, agents]
id: PB-171
epic: G — Burn Putt (pay-to-play mini golf)
priority: P2
status: todo
depends_on: [PB-170]
---

# PB-171 — Agent skill files, docs, rollout

Ship-the-feature chores. Plan §7, decision #1.

## Deliverables
- `src/frontend/public/llms-golf-local.txt` + `llms-golf-prod.txt` in the
  house skill format (mirroring `llms-rd-*.txt`): read `get_golf_info` /
  `get_golf_leaderboard`, the pay-to-play flow (deposit address → transfer
  fee + fees → `start_golf_round`), score submission shape, and explicit
  guardrails (fees non-refundable; fabricated logs get voided by replay
  validation; one active round per principal). Copy-instructions link on
  the game page.
- `docs/OPS.md` addendum: enabling/disabling `mini_golf`, changing the fee,
  pinning a new course version (and the leaderboard consequences), reading
  `golf_score_voided` audit events.
- Update `GOALS.md`-adjacent copy if needed (the game is a burn engine:
  every round converts 50% of its fee to cycles via the CMC).
- Rollout: deploy dark → enable `mini_golf` on local → owner playtest →
  owner decides production enablement (NEVER auto-enable in prod; mainnet
  deploys remain explicitly owner-initiated per standing rule).

## Acceptance Criteria
- [ ] Skill files served from the asset canister and accurate against the
      deployed candid (every command verified once via CLI).
- [ ] OPS runbook steps executed once on local.
- [ ] Flag remains OFF in any production deploy artifact.
