---
tags: [task, feature, casino, crash]
id: PB-230
epic: J — Crash / Casino
priority: P1
status: todo
depends_on: [PB-200]
---

# PB-230 — Casino VP ledger: rename to CASINO_VP_DELTA, house account, weekly burn

Plan: `plans/crash/` (decisions C1–C17).

## Scope (plans/crash/02; amends poker PB-200/doc 01)
- Rename POKER_VP_DELTA -> CASINO_VP_DELTA (same MemoryId 57); sanctioned
  writers grow to six (+crash settle, +house burn).
- House account = canister principal entry: absorbs each crash round's net
  (may go negative), excluded from effective_weight/votes/leaderboards.
- Weekly burn (Thursday sweep): positive house -> 0, lifetime-burned
  counter, audit `crash_house_burn`; negative carries (never minted away).
- Shared `reserved_chips(user)` helper (live crash bets vs poker
  sit-downs can't double-spend).
- **Tenure jubilee (D27/C18):** per-user `last_jubilee_period`; at each
  6-month tenure tick (lazily checked at settle/read) negative delta resets
  to 0 — writer #7; lifetime `jubilee_minted` counter; positive deltas
  never touched.
- `get_casino_reconciliation` (users + house + burned − jubilee_minted = 0
  forever), `get_casino_stats` (house balance, lifetime burned, jubilee).

## Acceptance (additional)
- Jubilee unit tests: fires at most once per period, only on negative
  deltas, restored base reflects the doubled tenure weight, reconciliation
  identity holds across burn + jubilee sequences.

## Acceptance
- I-1c/I-6/I-7 unit tests; burn idempotence; reservation cross-game test;
  house excluded from vote weight; poker tests still green post-rename.
