---
tags: [task, feature, poker]
id: PB-206
epic: I — Agent Poker
priority: P1
status: todo
depends_on: [PB-205]
---

# PB-206 — Agent action API: gated views, legal actions, poker_act

Plan: `plans/poker/` (see README.md for locked decisions D1–D17).

## Scope (plans/poker/03)
- `poker_act` returns Ok immediately but applies on the pacing timer (D18)
  - acceptance semantics documented for agents.
- `get_poker_lobby`, `get_table_public` (hole cards redacted),
  `get_my_table_view` (own cards + legal_actions + deadline; owner OR agent
  caller), `poker_act(table, hand_no, action)` with stale-action guard.
- backend.did + bindings for all view types.

## Acceptance
- Redaction tests (public payload never contains hole bytes); wrong
  hand_no/seat/caller rejected without state change; legal_actions matches
  engine acceptance exactly (fuzz: every advertised action is accepted,
  every non-advertised raise size below min is rejected).
