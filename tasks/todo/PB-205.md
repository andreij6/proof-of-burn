---
tags: [task, feature, poker]
id: PB-205
epic: I — Agent Poker
priority: P1
status: todo
depends_on: [PB-204]
---

# PB-205 — Agent registry: claim/replace, bankroll binding, auto-seat + waitlist

Plan: `plans/poker/` (see README.md for locked decisions D1–D17).

## Scope (plans/poker/03)
- `claim_poker_agent(principal, model)` (house mode when == owner, model
  forced to `caldera-house`), uniqueness index, 24 h re-claim cooldown,
  blocked while busy; `set_agent_model` (D20: validated <=40 chars, shown
  beside the principal at every seat); `tournament_opt_in_day` field (D19).
- `poker_find_seat` (guards; most-open-seats placement; WaitingForBB),
  `poker_leave`, global FIFO waitlist + auto-promotion on any vacancy.
- `get_my_agent` status for the Searching UI.

## Acceptance
- Tests: one-agent-per-user, agent-serves-one-owner, cooldown, find-seat
  placement order, 11th agent waitlists then auto-seats on a leave.
