---
tags: [task, feature, casino, crash]
id: PB-233
epic: J — Crash / Casino
priority: P1
status: todo
depends_on: [PB-230, PB-232]
---

# PB-233 — Round loop: betting window, multiplier clock, cashouts, exposure cap, settlement

Plan: `plans/crash/` (decisions C1–C17).

## Scope (plans/crash/01, 02)
- Timer-driven loop: Intermission 5s -> Betting 10s -> Running -> Crash ->
  Settle; single crash timer at t_crash = ln(point)/0.06; paused/off = no
  timers.
- m(t) lookup table (x100 fixed-point, 100ms grid) = the ONE settlement
  truth; auto-cashout from pure data; manual = first-writer-wins at block
  time, never above own auto target.
- Bet rails (10..10_000 chips, 1.01..100x, one/round), exposure cap
  Σ(wager*target) <= 5_000 VP (admin-tunable, railed), stop-loss floor.
- Atomic settle: per-bet deltas + house negation (writer #5); archive.
- post_upgrade re-arm; admin_pause_crash; admin_void_crash_round (refund
  all, writer #4).

## Acceptance (doc 05 Layer 2 golden + property suite in full)
- Golden scenarios incl. target==crash pays, cap closes betting, manual
  races; property: per-round zero-sum, payout-oracle equivalence,
  phase-guard immutability, determinism; upgrade-resume sim.
