---
tags: [task, feature, casino, crash]
id: PB-231
epic: J — Crash / Casino
priority: P1
status: todo
depends_on: []
---

# PB-231 — Crash domain types + storage + `crash` flag + Casino hub nav

Plan: `plans/crash/` (decisions C1–C17).

## Scope (plans/crash/01, 03)
- Types: CrashRound{phase, chain_index, seed_reveal, crash_x100,
  run_started_at, bets…}, CrashBet{user, wager, target_x100, result},
  ChatMsg, CrashStrategy, AutopilotState. impl_storable! for all.
- Stable: rounds archive (63), bets-by-round (64), chat ring (65),
  strategies+autopilot (66), house/burn bookkeeping (67).
- Flag `crash` (default OFF) in KNOWN_FEATURE_FLAGS + defaults.
- Casino hub: AppPage 'casino' (`/casino`, sub-tabs poker/crash by flags),
  `/poker` redirect; Play nav shows single Casino entry when either is on.

## Acceptance
- Compiles; flags listed; memory-id collision check vs 0-62; nav/redirect
  vitest (either flag on -> Casino visible; both off -> hidden+redirect).
