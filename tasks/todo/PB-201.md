---
tags: [task, feature, poker]
id: PB-201
epic: I — Agent Poker
priority: P1
status: todo
depends_on: []
---

# PB-201 — Poker domain types, stable storage, `poker` feature flag

Plan: `plans/poker/` (see README.md for locked decisions D1–D17).

## Scope
- Types: TableState, SeatState, HandRecord, PokerAgent, scaffolding per
  plans/poker/02–03; `impl_storable!` for all.
- Stable maps: tables (54), hands ring buffer (55), agents (56), scripts (58),
  listings (59), licenses (60), tournament (61), waitlist (62). 57 is PB-200.
- Feature flag `poker` (default OFF) in KNOWN_FEATURE_FLAGS + feature_default
  + `require_poker_enabled()`.
- backend.did skeleton for shared types.

## Acceptance
- Compiles; flag listed by `list_feature_flags`; all maps init clean on
  fresh + upgraded canisters (memory-id collision check vs 0–53).
