---
tags: [task, epic-h, docs, agents, ops]
id: PB-185
epic: H — AI Proposal Reviewer (DFINITY LLM canister)
priority: P2
status: todo
depends_on: [PB-184]
---

# PB-185 — Agent skills, OPS runbook, price migration, mainnet smoke checklist

Ship-the-feature chores. Plan §6.

## Deliverables
- Update `llms-local.txt` / `llms-prod.txt` (governance skills) with the
  AI-review flow: check cache via `get_ai_review` (free), pay-and-request
  via deposit address + `request_ai_review`, vote via `vote_ai_review`,
  reclaim via `refund_ai_deposit`. Note for agents: reviews are cached and
  public — check the cache before paying 3 ICP.
- `docs/OPS.md` addendum:
  - **Upgrade migration step:** existing canisters keep the old 0.05 ICP
    default — run `admin_set_ai_price '(300_000_000 : nat64)'` post-deploy
    (add to deploy-local.sh seed section and the prod checklist).
  - Kill switch: `admin_set_feature_flag '("ai_reviewer", false)'`.
  - Margin watch: DFINITY LLM canister is free today and may be metered
    later; `ai_price_e8s` is the lever.
- deploy-local.sh: idempotent `admin_set_ai_price` call AND
  `admin_set_feature_flag '("ai_reviewer", true)'` alongside the
  token-ledger wiring (the flag ships dark; local dev wants it on).
- Mainnet smoke + enablement checklist (owner-run, after an
  owner-initiated deploy, in this order): enable the flag → one real 3 ICP
  review on a live NNS proposal → record latency and output quality →
  verify readable from a second identity and an anonymous session →
  decide: leave enabled, or flip the flag back off.

## Acceptance Criteria
- [ ] Skill file commands verified once each via CLI on local.
- [ ] deploy-local.sh re-run shows the 3 ICP price in `get_config`.
- [ ] OPS runbook steps executed once on local; prod checklist written
      but NOT executed (mainnet deploys are owner-initiated only).
