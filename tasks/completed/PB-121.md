---
tags: [task, epic-h, frontend, nns, mainnet]
id: PB-121
epic: H — Frontend SPA
priority: P2
status: done
---

# PB-121 — Hardcoded neuron 4821667 in the help/"how it works" text

The neuron block + voting power read from `get_config()` (PB-116), but the help
modal still hardcoded `4821667`. With PB-119 fixed, config loads and the main
neuron block shows the real id; this fixes the remaining literal in the help copy.

## Fix
- Help text neuron id now renders `formatNeuronId(config?.primary_neuron_id)`
  (→ `17802688826615984104` on mainnet).

## Related
Balance/NNS question: the app uses your ICP **ledger balance at your app
principal's default account** (ICRC-1). You don't pre-deposit to the site — the
commit flow transfers from your wallet into a per-proposal escrow subaccount.
ICP held elsewhere (NNS neurons, other accounts) is not auto-visible.
