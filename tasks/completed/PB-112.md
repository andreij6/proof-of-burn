---
tags: [task, epic-j, testing, integration, pocketic]
id: PB-112
epic: J — Testing & QA
priority: P0
status: todo
depends_on: [PB-110, PB-111]
icp_skills: [canister-security]
---

# PB-112 — Real PocketIC integration tests for the burn saga

**The existing automated tests do not exercise the canister.** [[PB-090]]'s tests are pure-Rust unit tests (Storable roundtrips, constant checks, helper logic) and [[PB-092]] is a bash script requiring manual verification. There is **zero automated coverage** of `commit → settle → burn/refund`, access control, or eligibility at the canister boundary — exactly the high-risk surface that moves real ICP.

## Scope
Add a `tests/` crate using **PocketIC** with a local ICRC-1/2 ledger (and a stub CMC + stub NNS Governance) covering:

- **Access control:** anonymous rejected on every update; non-admin rejected on admin methods; admin add/remove + last-admin protection.
- **Eligibility:** `register_neuron` controller mismatch rejected; follow verification true/false; tier resolution 0–3.
- **Commit:** below-minimum, over-global-cap, over-stake-cap, already-committed, too-many-commitments, insufficient-deposit, commitment-closed (post-cutoff), commit allowed on `"met"` proposal (oversubscription).
- **Settlement — threshold met:** escrow → CMC burn → `notify_top_up` → `Burned`, vote cast, aggregates + audit log updated.
- **Settlement — threshold missed:** `abstained` → refund → `Returned`, net amount correct (minus ledger fee).
- **Saga failure/retry:** stub ledger/CMC that fails on first attempt; assert retry reaches a terminal state with **no double transfer** (validates [[PB-111]]).
- **NNS reject on mainnet ledger id:** assert no eligibility granted and no burn (validates [[PB-110]]).
- **Every `TransferError` branch** (BadFee, InsufficientFunds, TooOld, …) surfaces a typed error, not a panic.

## Done so far (`src/backend/tests/integration.rs`)
Real PocketIC harness running against the built wasm — **8 tests, green**.

Access-control / queries (5): anonymous ingress rejected; owner-is-admin +
`get_config` + `is_local`; non-admin `add_admin` rejected; anonymous tier 0;
seeded proposals listed.

Saga against the project's ICRC ledger (3):
- **commit happy-path**: deposit → commit → Pending, pots updated.
- **refund**: threshold missed → abstain → exact-target refund, escrow drained to 0.
- **burn idempotency ([[PB-111]])**: threshold met → vote → burn; CMC notify
  fails → FailedBurn with block index persisted; retry does **not** transfer to
  the CMC twice (asserts no double-spend).

To make the ledger installable at a PocketIC-assigned id, `InitPayload` gained an
optional `ledger_canister_id` override (absent → derived from `is_local`).

Run: `cargo build --target wasm32-unknown-unknown --release -p backend` then
`cargo test -p backend` (auto-discovers the dfx PocketIC binary, or set
`POCKET_IC_BIN`). Skips gracefully if wasm/binary absent.

**This fix surfaced a real settlement bug**: `if let Some(mut agg) =
map.borrow().get(&user)` held the immutable RefCell borrow across the body, so
the inner `borrow_mut()` trapped — breaking *all* burn/refund settlement. Fixed
in both branches (release the borrow before mutating).

## Remaining (smaller, non-blocking)
- Burn **success** path needs a stub CMC at `rkp4c-7iaaa-aaaaa-aaaca-cai`
  returning `notify_top_up` Ok (currently only the CMC-failure/idempotency path
  is asserted).
- A stub NNS `list_proposals` to cover [[PB-117]] live ingestion and a divergent
  `nns_proposal_id` vote ([[PB-115]]).

## Acceptance criteria
- [x] Access-control + query boundary covered by a green PocketIC suite.
- [x] Refund lifecycle passes end-to-end against a real ICRC ledger.
- [x] Burn lifecycle reaches the CMC and is retry-idempotent (no double-spend).
- [x] Ledger balances and commitment statuses match expectations per scenario.
- [ ] Burn *success* (stub CMC) — future scope.
