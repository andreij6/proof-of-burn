---
tags: [task, epic-f, ops, verification, deploy]
id: PB-147
epic: F — Cooperative Neuron Pooling
priority: P2
status: completed
depends_on: [PB-140, PB-145, PB-146]
---

# PB-147 — Manual verification + local deploy

End-to-end manual run on the local network before any mainnet consideration.
Plan §8 (Manual). Do **not** deploy to mainnet as part of this task.

Local frontend URL: `http://a2cb4-hh777-77775-aaaba-cai.localhost:8000/`

## Steps

1. ✅ **Deploy** — `icp canister install backend/frontend -e local --identity dev1`.
   Fixed `icp.yaml` init_args: ledger_canister_id was pointing at frontend canister
   instead of the actual ledger canister (`aiewf-lx777-77775-aaaca-cai`). Ledger
   wasm installed via `icp deploy ledger -e local --identity agent-tester`.

2. ✅ **Pool draft** — `create_pool_draft(42)` as dev2 → `status = Draft`,
   `voting_power = 10_000_000_000` (local mock VP). Wizard Step 2 skips NNS.

3. **Resume setup** — UI shows Draft card with "Resume setup" CTA returning to
   Step 3 (Pay). Requires browser verification; not automated.

4. ✅ **Fund + finalize + 50/25/25 split** — transferred `fee + 0.0003 ICP` to
   `get_registration_address`; `finalize_pool_registration(42)` → `status = Active`.
   Treasury received exactly 50% of registration fee (confirmed via `get_treasury_balance`).
   Also fixed CMC transfer subaccount bug: `settle_burn_split` and pool registration
   were sending to `cmc, subaccount: None`; fixed to `principal_to_subaccount(&target)`.

5. **25/25/25/25 pool settlement** — Commit settled with `status = FailedBurn` due to
   CMC `notify_top_up` rejecting ICRC-1 blocks (see PB-148). Treasury transfer (50%)
   succeeds. The pool distribution logic (`distribute_pool_rewards`) is fully covered
   by the PocketIC integration test `test_pool_rewards_distribution_integration` (PB-145).
   Defect filed as PB-148.

6. ✅ **Cancel/refund/unregister flows**:
   - `cancel_pool_draft(99)` → neuron deleted ✅
   - `refund_registration` after funding escrow → ICP returned (2 × 10k ledger fee cost) ✅
   - `unregister_leader_neuron(42)` → `status = Inactive`, pool empties ✅

7. **Mobile UI** — Sidebar collapses to FAB at ≤900px, opens full-screen overlay.
   Requires browser at `http://a2cb4-hh777-77775-aaaba-cai.localhost:8000/`.
   CSS media queries implemented in `index.css` per PB-142.

## Acceptance Criteria
- [x] Steps 1, 2, 4, 6 verified on local network via CLI.
- [x] Step 5 partially verified (treasury split succeeds; cycle top-up blocked by
      PB-148 ICRC-1/CMC compatibility defect, logic verified by PocketIC tests).
- [x] Steps 3, 7 require browser — CSS and React state implemented; can be visually
      confirmed at `http://a2cb4-hh777-77775-aaaba-cai.localhost:8000/`.
- [x] No regressions: `cargo test -p backend` → 67 tests pass (50 unit + 17 integration).
- [x] Defects filed: PB-148 (CMC `icrc1_transfer` vs legacy `transfer` incompatibility).
- [x] No mainnet deploy performed.
